name: Deploy to VPS

on:
  push:
    branches:
      - main
      - 'claude/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 31.177.82.167
          username: root
          password: ${{ secrets.SSH_PASSWORD }}
          script: |
            cd /opt/crm
            BRANCH="${{ github.ref_name }}"
            sudo -u crm git fetch --all --prune
            sudo -u crm git reset --hard origin/$BRANCH
            bash deploy/deploy.sh
